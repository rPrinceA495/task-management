import React from 'react';
import ReactLoader from 'react-loader';

const Loader = () => (
  <ReactLoader
    lines={13}
    length={28}
    width={14}
    radius={42}
    color="#999999" />
);

export default Loader;
